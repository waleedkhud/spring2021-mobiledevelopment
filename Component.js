// Component is the parent
class Component {
  constructor (props, state) {
    this.props = props;
    this.props = state
  }
  //we state the components are the object

  render () {
    // Child objects define their own render() methods
  }
}

export default Component
